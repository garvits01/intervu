# Product Backlog — Haveloc Pro (AI-Powered Campus Placement & Project Management Platform)

| ID | Title | Epic | User Story | Priority (MoSCoW) | Status | Acceptance Criteria | Functional Requirements | Non-Functional Requirements | Original Estimate | Actual Effort (In days) |
|----|-------|------|------------|-------------------|--------|--------------------|-----------------------|---------------------------|-------------------|------------------------|
| 1 | User Authentication & RBAC | Authentication | As a user, I want to securely log in with my email and password so that my personal data and organizational information remain protected | Must | Completed | 1. User can register with valid email and password. 2. User can log in and receive JWT access + refresh tokens. 3. Role-based access (SUPER_ADMIN, ADMIN, MEMBER, VIEWER) restricts features. 4. Password is securely hashed using scrypt/bcrypt. | Implement NextAuth credentials provider on frontend. Build NestJS auth module with JWT token generation. Hash passwords using scrypt (seed) and bcrypt (registration). Implement role-based route guards. | Response time for authentication actions should be less than 2 seconds. Passwords must use scrypt/bcrypt with minimum 10 salt rounds. JWT tokens must expire within 1 hour (access) and 7 days (refresh). | 5 days | 4 days |
| 2 | Dashboard & Analytics Overview | Dashboard | As an admin, I want a centralized dashboard showing project KPIs, task summaries, and placement metrics so that I can monitor overall performance at a glance | Must | Completed | 1. Dashboard displays active projects count, task distribution, and placement statistics. 2. Real-time notification feed shows AI insights, task overdue alerts, and placement updates. 3. Data refreshes on page load with latest information. | Build dashboard page with project cards, task metrics, and drive summaries. Fetch data from API endpoints for projects, tasks, and placement drives. Display notification feed with categorized alerts. | Page load time should be less than 2 seconds. Dashboard should render within 1.5s (LCP). Data must be fetched from API within 200ms (p99). | 7 days | 6 days |
| 3 | Project & Task Management | Project Management | As a project lead, I want to create projects, assign tasks with priorities, and track progress through Kanban views so that my team stays organized and deadlines are met | Must | Completed | 1. Users can create projects with name, description, start/end dates. 2. Tasks support statuses (TODO, IN_PROGRESS, IN_REVIEW, BLOCKED, DONE). 3. Tasks have priority levels (CRITICAL, HIGH, MEDIUM, LOW). 4. Task dependencies can be defined (Finish-to-Start, Start-to-Start). 5. Milestones can be set for project tracking. | Build project CRUD with organization scoping. Implement task management with assignee, creator, priority, due date, and tags. Support task dependencies via TaskDependency model. Create milestone tracking per project. | Tasks should load within 1 second per project. Support up to 10,000 tasks per project without performance degradation. All task state changes must be persisted within 500ms. | 10 days | 9 days |
| 4 | Placement Drive Management | Placement | As a campus recruiter, I want to create and manage placement drives with lifecycle tracking so that I can efficiently coordinate hiring events across multiple companies | Must | Completed | 1. Drives can be created with title, description, start/end dates. 2. Drive lifecycle tracks: DRAFT → REGISTRATION_OPEN → IN_PROGRESS → EVALUATION → COMPLETED. 3. Candidates can register for drives. 4. Jobs are linked to specific drives with requirements and skills. | Create PlacementDrive, Job, and DriveRegistration CRUD endpoints. Implement drive status lifecycle management. Support job listings with company, requirements, skills, salary, and job type (FULL_TIME, INTERNSHIP, CONTRACT). Track candidate registrations per drive. | Drive creation should complete within 1 second. Support up to 500 concurrent registrations per drive. Drive dashboard should display real-time registration counts. | 8 days | 7 days |
| 5 | AI-Powered Candidate–Job Matching | AI Matching | As a campus recruiter, I want AI to automatically match candidates to job openings based on skills and qualifications so that placement rates increase and manual screening time drops by 80% | Must | Completed | 1. AI parses candidate resumes to extract skills, education, and experience. 2. Match scores (0–1) and confidence levels are generated per candidate-job pair. 3. Bias flags are detected and reported for audit. 4. Match results can be PENDING, APPROVED, REJECTED, or OVERRIDDEN. | Build AI service (Python/FastAPI) for resume parsing using NLP. Implement matching algorithm with score + confidence calculation. Create MatchResult model with driveId, jobId, candidateId, score, confidence, reasoning, and biasFlags. Support human-in-the-loop review for match approval. | AI matching for 500 resumes against 20 JDs must complete within 60 seconds. Match confidence threshold should be configurable. Bias detection must flag disparities > 2% for audit. | 12 days | 10 days |
| 6 | Candidate Profile & Resume Parsing | Candidate Management | As a student, I want to create my profile and upload my resume so that the system can parse my skills and match me to relevant opportunities | Must | Completed | 1. Candidates can upload resumes (PDF/DOCX). 2. System extracts skills, education, experience via NLP. 3. Parsed data is stored as structured JSON. 4. Candidate profile shows parsed skills array and education history. | Create CandidateProfile model linked to User. Implement resume upload to S3 with file URL storage. Build NLP-based parser extracting parsedSkills, education, experience. Store embeddings for vector similarity matching. | Resume parsing should complete within 5 seconds per document. Support file sizes up to 10MB. Parsed skills accuracy should exceed 90%. | 7 days | 6 days |
| 7 | Topic-Based Collaboration Threads | Collaboration | As a team member, I want topic-specific discussion threads attached to projects so that conversations remain contextual, searchable, and organized | Should | Completed | 1. Threads can be created within projects with title and topic. 2. Threads support pinning for important discussions. 3. AI-generated summaries highlight key decisions and action items. 4. Team members can post comments within threads. | Build Thread model with projectId, title, topic, isPinned, and aiSummary. Implement Comment model for threaded discussions. AI summary generation from thread content via LLM API. Support thread search and filtering. | Thread loading should complete within 1 second. AI summary generation should complete within 5 seconds. Support 500+ messages per thread without UI lag. | 6 days | 5 days |
| 8 | Notification & Alert System | Notifications | As a user, I want to receive real-time notifications for task assignments, placement updates, and AI-generated insights so that I stay informed without manually checking each module | Must | Completed | 1. Notifications support types: TASK_ASSIGNED, TASK_UPDATED, TASK_OVERDUE, COMMENT_ADDED, MENTION, PLACEMENT_UPDATE, AI_INSIGHT, SYSTEM. 2. Notifications show read/unread status. 3. Users receive in-app alerts for all relevant events. | Build Notification model with userId, type, title, body, data, and isRead. Create notification creation hooks for task, placement, and AI events. Implement mark-as-read functionality. Index on userId + isRead for fast queries. | Notifications should appear within 2 seconds of event trigger. Support 1000+ unread notifications per user without query degradation. Notification list should load within 500ms. | 5 days | 4 days |
| 9 | Template Marketplace | Templates | As a new organization onboarding to Haveloc Pro, I want pre-built templates for projects, drives, reports, and communications so that I can launch my first drive in under 1 hour | Should | In Progress | 1. Templates available for categories: PROJECT, PLACEMENT_DRIVE, TASK_BOARD, REPORT, WORKFLOW, COMMUNICATION. 2. Templates are organization-scoped or public. 3. Usage count tracks popularity. 4. Templates can be cloned and customized. | Build Template model with orgId, name, category, content (JSON), isPublic, and usageCount. Create template browsing and filtering by category. Implement template cloning with customization. Track usage analytics per template. | Template listing should load within 1 second. Template cloning should complete within 2 seconds. Support 500+ templates per organization. | 6 days | 5 days |
| 10 | Audit Logging & Activity Tracking | Security & Compliance | As an admin, I want a comprehensive audit trail of all actions performed in the system so that I can ensure compliance, track changes, and investigate incidents | Should | Completed | 1. All CRUD operations are logged with before/after state. 2. Audit logs capture userId, action, entity, entityId, IP address, and user agent. 3. Activity logs track user actions on tasks. 4. Logs are indexed by orgId and timestamp for fast querying. | Build AuditLog model with orgId, userId, action, entity, entityId, before/after JSON, ipAddress, userAgent. Build ActivityLog model for task-level user actions. Index on [orgId, createdAt] and [entity, entityId]. Implement audit log API endpoints with pagination and filtering. | Audit log writes must not add more than 50ms to request latency. Logs should support 7-year retention. Query performance should remain under 1 second for 1M+ log entries. | 5 days | 4 days |
